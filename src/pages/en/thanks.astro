---
import Layout from '../../layouts/Layout.astro';
import BareLayout from '../../layouts/BareLayout.astro';
import ThanksPage from '../../components/ThanksPage.astro';
import Construction from '../../components/Construction.astro';
import { useTranslations } from '../../i18n/utils';
import { settings } from '../../config/settings';

const t = useTranslations('en');
---

{
	settings.inConstruction ? (
		<BareLayout lang="en" title={t.construction.metaTitle} description={t.construction.metaDescription}>
			<Construction lang="en" />
		</BareLayout>
	) : (
		<Layout lang="en" title={t.thanks.metaTitle} description={t.thanks.body} noindex>
			<ThanksPage lang="en" />
		</Layout>
	)
}
